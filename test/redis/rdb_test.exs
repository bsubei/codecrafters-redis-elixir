defmodule Redis.RDBTest do
  use ExUnit.Case
  doctest Redis.RDB

  describe "decode RDB data" do
    test "given invalid data should error out" do
    end

    test "given empty RDB file" do
    end

    test "given non-empty RDB file" do
    end
  end
end
